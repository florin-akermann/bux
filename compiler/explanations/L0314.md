# L0314 — a name that binds is written inside an or-pattern

`A | B` matches what `A` matches and what `B` matches, and it binds nothing at all.

```text
match status {
    Failed(reason) | Cancelled(reason) => reason
    Done => "done"
}
```

`reason` binds, so this is refused where the first one is written.

An or-pattern that bound something would have to bind the same names at the same types in every
alternative.
That is a rule about the set of alternatives rather than about each of them, and it is machinery
no example in hand asks for.
`docs/specs/patterns.md` states the rule and why it is the narrow one.

The arms it would have saved are written as one arm each:

```text
match status {
    Failed(reason) => reason
    Cancelled(reason) => reason
    Done => "done"
}
```

A pattern that binds nothing is written inside an or-pattern as freely as anywhere else.
A variant carrying nothing, a literal, and `_` are each one of those, so `Pending | Running` and
`0 | 1` are both written.
A record pattern binds every field it names, so one is never an alternative.
