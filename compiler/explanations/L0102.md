# L0102 — a character is not part of the language

The character the carets point at begins no token of the language.

Bux is written in ASCII outside of string literals and comments.
A character such as `@`, `#`, or a smart quote pasted from a document means nothing in source.
Delete it, or move it inside a string.
