# L0104 — a backslash is followed by something that is not an escape

Inside a string a `\` introduces an escape, and the language has five of them.
They are `\"`, `\\`, `\n`, `\t`, and `\r`.
Anything else after a `\` is refused rather than guessed at.

A backslash that belongs in the text is written `\\`.
