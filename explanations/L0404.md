# L0404 — a record is built without one of its fields

Building a record gives a value to every field the record declares.
Bux has no default value and no null, so a field left out has nothing to hold.

Updating a record is the way to write only the fields that change.
`user { active: false }` keeps every other field of `user` as it was.
