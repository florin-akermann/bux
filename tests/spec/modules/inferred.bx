// expect-run
// > 2
// > passed
// A function whose type nothing settled is generic too, and is reached exactly as one that
// writes its type parameters out is.
import holder
import io

fn main(arguments: List<String>) -> Int {
    io.println(shown(holder.passed(2)))
    io.println(holder.passed("passed"))
    0
}
