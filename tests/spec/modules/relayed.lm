// expect-run
// > relayed
// A value of a type this module does not import is held and read as what it was declared as.
import io

import relay

fn main(arguments: List<String>) -> Int {
    io.println(relay.got().name)
    0
}
