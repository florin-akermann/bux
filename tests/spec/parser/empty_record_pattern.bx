// expect-error: L0100
// A record pattern names at least one field; `_` is the pattern that looks at nothing.
fn sized(point: Point) -> Int {
    match point {
        Point {} => 1
    }
}

type Point = {
    x: Int
}
