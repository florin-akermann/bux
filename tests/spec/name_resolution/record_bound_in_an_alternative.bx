// expect-error: L0314
// A record pattern binds every field it names, so one that names a field is never an alternative.
fn described(outcome: Outcome) -> String {
    match outcome {
        Ready | Held { held } => "there"
    }
}

type Outcome =
    | Ready
    | Held {
        held: String
    }
