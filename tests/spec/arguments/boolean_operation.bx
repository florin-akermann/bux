// expect-run
// A function whose parameters and result are all `Bool` is about `Bool`, so its operands stay.
fn main(arguments: List<String>) -> Int {
    if is_implied(first: true, second: true) && is_both(left: true, right: true) {
        return 0
    }
    0
}

// Each parameter and the result are `Bool`, so the parameters are operands, not choices.
// example: is_implied(first: true, second: false) == false
fn is_implied(first: Bool, second: Bool) -> Bool {
    !first || second
}

// The exception applies to the signature, so a repeated type still makes the call name them.
// example: is_both(left: true, right: false) == false
fn is_both(left: Bool, right: Bool) -> Bool {
    left && right
}
