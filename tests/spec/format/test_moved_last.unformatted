// `bux fmt` moves a declaration written after a test above the tests, and keeps the tests in the
// order written.
test "counts" {
    total() == 2
}

test "is positive" {
    total() > 0
}

/// The number two.
fn total() -> Int {
    2
}
