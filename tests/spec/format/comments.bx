// What this file is for.
import io

fn go() -> Int {
    // start at nothing
    let total = 0
    // around again
    for {
        io.println("once")
        break
        // nothing more
    }
    total
}

// that is all
