// expect-error: L0101
// A carriage return before a line break is a blank, so a comment and a string stop before it.
fn f() {
    let name = "Alice
    let other = "a backslash does not escape a line break\
}
