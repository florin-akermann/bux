// expect-error: L0435
// A variant holds no foreign reference either, whether it holds one directly or in an `Option`.
fn main(arguments: List<String>) -> Int {
    0
}

/// A file that a program found, or found not to be there.
type Found =
    | There(Option<File>)
    | Missing

extern type File = "java.io.File"
