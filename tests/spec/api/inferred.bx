// A function writes its signature, and inference does the work inside the body.
// `bux api` prints the type each name has, which is the type its signature writes.
fn go() -> Int {
    twice(3)
}

fn twice(count: Int) -> Int {
    count + count
}
