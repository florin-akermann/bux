// expect-run
// > 3
// > 0
// `or(maybe, fallback)` is the one spelling of a fallback: the arguments go inside the parentheses.
import io

fn main(arguments: List<String>) -> Int {
    io.println(shown(or(shared(total: 17, people: 5), 0)))
    io.println(shown(or(shared(total: 17, people: 0), 0)))
    0
}

/// What each of `people` gets of `total`, which is nothing at all when there is nobody.
// example: or(shared(total: 17, people: 5), 0) == 3
// example: or(shared(total: 17, people: 0), 9) == 9
fn shared(total: Int, people: Int) -> Option<Int> {
    total / people
}
