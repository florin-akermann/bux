// expect-error: L0805
// A process has one type of state and one type of message, so no function of it is generic.
fn main(arguments: List<String>) -> Int {
    _ = spawn Counter()
    0
}

process Counter {
    fn start<T>() -> Int {
        0
    }

    fn receive(total: Int, message: Counted) -> Next<Int> {
        match message {
            Add(amount) => Continue(total + amount)
            Stop => Done
        }
    }
}

type Counted =
    | Add(Int)
    | Stop
