// expect-error: L0419
// A signature has no body, so a parameter of one states its type where it is written.
fn main(arguments: List<String>) -> Int {
    0
}

trait Area<T> {
    fn covered(shape) -> Int
}
