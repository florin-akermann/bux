// expect-error: L0315
// A manifest states `package`, then `version`, then a `depends` for each dependency, then a `jar`
// for each archive, and the one of this package states something else.
fn held() -> Int {
    1
}
