[package]
name = "lumen-ir"
description = "Lowering: a checked module as the classes a JVM loads"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[test]]
name = "integration"
path = "tests/integration/main.rs"

[dependencies]
lumen-ast = { path = "../ast" }
lumen-holes = { path = "../holes" }
lumen-resolver = { path = "../resolver" }
lumen-types = { path = "../types" }

[dev-dependencies]
hegeltest = "0.23" # approved: property-based testing, required by AGENTS.md for every phase
lumen-parser = { path = "../parser" }

[lints]
workspace = true
