[package]
name = "lumen-parser"
description = "Parser: tokens to the untyped abstract syntax tree"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[test]]
name = "integration"
path = "tests/integration/main.rs"

[dependencies]
lumen-ast = { path = "../ast" }
lumen-lexer = { path = "../lexer" }

[dev-dependencies]
hegeltest = "0.23" # approved: property-based testing, required by AGENTS.md for every phase

[lints]
workspace = true
