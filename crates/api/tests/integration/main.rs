//! The one integration-test binary of the `lumen-api` crate.
//!
//! Every test file is a module declared here; shared helpers go in a `common` module.

mod common;
mod examples;
mod properties;
mod surface;
