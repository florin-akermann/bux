[package]
name = "lumen-modules"
description = "Loading: every module a program reaches, read in the order they are compiled"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[test]]
name = "integration"
path = "tests/integration/main.rs"

[dependencies]
lumen-ast = { path = "../ast" }
lumen-diagnostics = { path = "../diagnostics" }
lumen-parser = { path = "../parser" }
lumen-resolver = { path = "../resolver" }
lumen-types = { path = "../types" }

[dev-dependencies]
hegeltest = "0.23" # approved: property-based testing, required by AGENTS.md for every phase

[lints]
workspace = true
