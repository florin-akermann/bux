[package]
name = "lumen-diagnostics"
description = "Diagnostics: the codes the compiler refuses with, and how a refusal reads"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[test]]
name = "integration"
path = "tests/integration/main.rs"

[dependencies]
lumen-lexer = { path = "../lexer" }

[dev-dependencies]
hegeltest = "0.23" # approved: property-based testing, required by AGENTS.md for every phase

[lints]
workspace = true
