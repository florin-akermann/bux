# L0105 — comparisons are chained

`a < b < c` reads in mathematics as two comparisons joined by "and".
In most languages it means something else entirely.
It compares `a < b`, and then compares that answer with `c`.

Lumen refuses the shape rather than picking one of the two meanings.
Write what you mean: `a < b && b < c`.
