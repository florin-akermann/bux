# L0101 — a string has no closing quote

A string literal begins and ends on one line.
A `"` that is never closed before the end of that line is an error, not a string that runs on.

Add the closing `"`.
A quote that belongs in the text is written `\"`, and a line break in it is written `\n`.
