# L0403 — a type would contain itself

Inference was asked to make a type equal to a type that holds it, such as `T` and `List<T>`.
No finite type satisfies that, so there is nothing to infer.

This is almost always a value used as two different things.
A loop binding used where the list it came from was meant is the usual way to write it.
