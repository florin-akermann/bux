//! The one integration-test binary of the `lumen-diagnostics` crate.
//!
//! Every test file is a module declared here; shared helpers live in `common`.

mod catalogue;
mod common;
mod properties;
mod rendering;
