//! The one integration-test binary of the `lumen-format` crate.
//!
//! Every test file is a module declared here; shared helpers live in `common`.

mod comments;
mod common;
mod examples;
mod expressions;
mod gate;
mod items;
mod meaning;
mod properties;
mod statements;
