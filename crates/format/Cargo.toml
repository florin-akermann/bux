[package]
name = "lumen-format"
description = "Formatter: the one canonical form of a program, and the gate that requires it"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[test]]
name = "integration"
path = "tests/integration/main.rs"

[dependencies]
lumen-ast = { path = "../ast" }
lumen-lexer = { path = "../lexer" }
lumen-parser = { path = "../parser" }

[dev-dependencies]
hegeltest = "0.23" # approved: property-based testing, required by AGENTS.md for every phase

[lints]
workspace = true
