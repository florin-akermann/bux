[package]
name = "lumen-resolver"
description = "Name resolution: every name in a module pointed at the definition it means"
version.workspace = true
edition.workspace = true

[lib]
path = "src/lib.rs"

[[test]]
name = "integration"
path = "tests/integration/main.rs"

[dependencies]
lumen-ast = { path = "../ast" }
lumen-diagnostics = { path = "../diagnostics" }
lumen-parser = { path = "../parser" }

[dev-dependencies]
hegeltest = "0.23" # approved: property-based testing, required by AGENTS.md for every phase

[lints]
workspace = true
